{
  "name": "protect-life-above-land-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend/Iteration2 && npm install && npm run build"
  }
}
